# =====================================================
# CONFIGURACIÓN DEL BACKEND - DELIVERY ROUTING
# =====================================================

# Entorno
ENVIRONMENT=development
HOST=0.0.0.0
PORT=3000

# Base de datos
DATABASE_URL=postgresql://delivery_user:password@localhost:5432/delivery_routing

# Redis
REDIS_URL=redis://localhost:6379

# JWT
JWT_SECRET=your-secret-key-here
JWT_EXPIRATION=86400

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://192.168.1.9:3000,http://192.168.1.146:3000

# Rate Limiting
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=3600

# Mapbox (opcional)
MAPBOX_TOKEN=your_mapbox_token_here

# =====================================================
# COLIS PRIVÉ API - URLs OFICIALES
# =====================================================
# Estas son las URLs oficiales de Colis Privé
# No contienen información sensible, solo endpoints públicos

COLIS_PRIVE_AUTH_URL=https://wsauthentificationexterne.colisprive.com
COLIS_PRIVE_TOURNEE_URL=https://wstournee-v2.colisprive.com
COLIS_PRIVE_DETAIL_URL=https://wstournee-v2.colisprive.com
COLIS_PRIVE_GESTION_URL=https://gestiontournee.colisprive.com
COLIS_PRIVE_REFERENTIEL_URL=https://wsreferentiel-v2.colisprive.com/WS_RefDistributeur/RefDistributeurConsolideExtranetToExterne.svc

# =====================================================
# CREDENCIALES COLIS PRIVÉ (NO HARDCODEADAS)
# =====================================================
# Estas se envían dinámicamente via HTTP requests
# No se almacenan en variables de entorno por seguridad

# COLIS_PRIVE_USERNAME=se_envia_via_request
# COLIS_PRIVE_PASSWORD=se_envia_via_request
# COLIS_PRIVE_SOCIETE=se_envia_via_request
